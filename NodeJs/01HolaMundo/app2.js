const saludar = (nombre) => { // funcion de flecha que recibe un parametro nombre

    return `Hola ${nombre}` 
    // retorna un string con el parametro nombre, el return se usa para retornar un valor y 
    //retorna valor por defecto undefined

};

console.log(saludar("moises")) // imprime en consola el valor de la funcion saludar con el parametro moises

